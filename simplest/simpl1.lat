class list {
    list next;
    int elem;
}


int main() {
  list l;
  l = new list;
  l.elem = 1;
  int x = l.elem;
  printInt(x);

  list l2 = new list;
  l2.elem = 2;
  printInt(l2.elem);

  l2.next = l;
  int y = l2.next.elem;
  printInt(y);
  return 0;
}


