class list {
    list next;
    int elem;
}

list append(list xs) {
  list head = new list;
  head.elem = xs.elem + 1;
  xs.next = head;
  return head;
}

int main() {
  list l1 = new list;
  l1.elem = 1;
  list l2 = append(l1);

  printInt(l2.elem);
  printInt(l1.next.elem);

  return 0;
}


