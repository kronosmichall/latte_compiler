int two() {
    return 2;
}

int three() {
    int a = 2;
    return a + 1;
}

int four() {
    int a = 4;
    return a;
}

int five() {
    int x = 2;
    x = x + 3;
    return x;
}

int main() {
	int x = 10;
    int y = x - 9;
    // int z = mul(x + 2 + 1, y + 1);  
    printInt(x);
    printInt(y);
    printInt(two() + three() + four());
    // printInt(z);

    //string y = "abc";
    return 0;
}

// int mul(int a, int b) {
//     x = a * b;
//     return x;
// }
